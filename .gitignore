# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler / Ruby (github-pages gem is a moving target — resolve fresh per machine)
Gemfile.lock
vendor/

# Python previewer scratch
__pycache__/
